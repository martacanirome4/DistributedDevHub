Este proyecto consiste en una aplicación cliente-servidor que utiliza FIFOs (tuberías con nombre) de Unix para transferir archivos entre el cliente y el servidor. El cliente se conecta al servidor y envía el path completo de un archivo. El servidor responde con los contenidos de 
## Requisitos Previos
- Python 3.x
- Sistema operativo basado en UNIX (Linux, macOS)

## Estructura del Proyecto
ped7/
└── p3/
    └── yolanda/
        └── app2/
            ├── serv3.py
            ├── cli3.py
            ├── MAKEFILE
            └── README.mds

## Instrucciones para Ejecutar el Proyecto

1. Clonar el Repositorio
Clona este repositorio en tu máquina local:
    ```bash
    git clone http://git.eps.ceu.es/ped/ped7
    ```
2. Ejecutar el Servidor

  
    ```bash
    python3 serv3.py
    ```

   Ejecuta el cliente 
ó
    ```bash
    python3 cli3.py
    ```


